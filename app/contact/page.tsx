import type { Metadata } from "next";
import ContactForm from "./ContactForm";
import Footer from "@/app/components/Footer";
import Navbar from "@/app/components/Navbar";
import { buildPageMetadata } from "@/lib/seo";
import { getSiteSettings } from "@/lib/site-settings";
export async function generateMetadata(): Promise<Metadata> { const settings = await getSiteSettings(); return buildPageMetadata({ title: "Contact Helios | Real Estate Media", description: "Contact Helios Real Estate Media for general questions, partnerships, billing, or support with an existing project.", path: "/contact", settings }); }
export default function ContactPage() { return <main className="min-h-screen bg-[#090909] text-white"><Navbar /><section className="relative overflow-hidden border-b border-white/[0.08] pt-28"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_80%_15%,rgba(217,107,43,0.17),transparent_34%)]" /><div className="hero-grain pointer-events-none absolute inset-0 opacity-[0.03]" /><div className="container-shell relative grid gap-14 py-20 sm:py-28 lg:grid-cols-[minmax(0,0.75fr)_minmax(34rem,1.25fr)] lg:gap-20"><div><p className="eyebrow text-[var(--helios-orange)]">Contact Helios</p><h1 className="mt-7 font-display text-[clamp(3.8rem,7vw,7rem)] font-light leading-[0.87] tracking-[-0.06em]">Have a question? Let&apos;s talk.</h1><p className="mt-8 max-w-lg text-base leading-8 text-white/42">For general questions, partnerships, billing, or help with an existing project, send us a message here. Ready to schedule media? Use Book Your Shoot instead.</p></div><ContactForm /></div></section><Footer /></main>; }
