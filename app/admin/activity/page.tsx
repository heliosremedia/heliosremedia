import { prisma } from "@/lib/prisma";

export const dynamic = "force-dynamic";

export default async function ActivityPage() {
  const events = await prisma.auditEvent.findMany({ take: 150, orderBy: { createdAt: "desc" }, include: { actor: { select: { displayName: true } } } });
  return <div className="space-y-7"><section className="border-b border-white/[0.08] pb-7"><p className="eyebrow text-[var(--helios-orange)]">Security and accountability</p><h1 className="mt-3 text-3xl font-light tracking-[-0.03em] text-white sm:text-4xl">Activity log</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/40">A durable record of authentication and administrative actions across the Helios workspace.</p></section><section className="overflow-hidden rounded-2xl border border-white/[0.08] bg-[#111]"><div className="grid grid-cols-[10rem_minmax(0,1fr)] gap-4 border-b border-white/[0.08] px-5 py-4 text-[0.52rem] uppercase tracking-[0.15em] text-white/25 sm:grid-cols-[12rem_11rem_minmax(0,1fr)]"><span>When</span><span className="hidden sm:block">Actor</span><span>Event</span></div>{events.map((event) => <article key={event.id} className="grid grid-cols-[10rem_minmax(0,1fr)] gap-4 border-b border-white/[0.06] px-5 py-5 last:border-0 sm:grid-cols-[12rem_11rem_minmax(0,1fr)]"><time className="text-xs leading-5 text-white/30">{event.createdAt.toLocaleString("en-US", { dateStyle: "medium", timeStyle: "short" })}</time><div className="hidden min-w-0 sm:block"><p className="truncate text-xs text-white/50">{event.actor?.displayName || event.actorEmail || "System"}</p><p className="mt-1 truncate text-[0.52rem] uppercase tracking-[0.1em] text-white/20">{event.ipAddress || "No IP recorded"}</p></div><div><p className="text-[0.52rem] font-semibold uppercase tracking-[0.13em] text-[var(--helios-orange)]">{event.action.replaceAll("_", " ")}</p><p className="mt-2 text-sm leading-6 text-white/48">{event.summary}</p></div></article>)}{events.length === 0 && <div className="py-20 text-center"><p className="font-display text-3xl text-white/25">No activity recorded yet.</p><p className="mt-3 text-sm text-white/20">Security events will appear after authentication is configured.</p></div>}</section></div>;
}
